export function DashboardPage() {
  return (
    <section className="dashboard-empty">
      <div>
        <p className="eyebrow">Workspace</p>
        <h1>Dashboard</h1>
        <p>
          We will build the dashboard after the backend data model is ready.
          For now, use folders to shape the research workspace.
        </p>
      </div>
    </section>
  )
}
